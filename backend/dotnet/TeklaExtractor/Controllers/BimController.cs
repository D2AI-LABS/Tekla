// BimController.cs — Reserved for future HTTP controller if upgrading to ASP.NET
// Currently the .NET plugin communicates via file-based IPC (bim_elements.json)
// and does not expose HTTP endpoints.
namespace TeklaExtractor.Controllers
{
    // Placeholder — extend this class to add HTTP endpoints
    public class BimController { }
}
